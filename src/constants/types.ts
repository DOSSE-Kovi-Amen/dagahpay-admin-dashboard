export interface Credentials {
    email: string;
    password: string;
}
export interface Register {
    firstname: string;
    lastname: string;
    email: string;
    password: string;
}
export interface User {
    firstname: string;
    lastname: string;
    email: string;
}